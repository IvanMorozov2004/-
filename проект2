Задание 5
SELECT * FROM project_simple.project;
SELECT COUNT(1) FROM project_simple.project;

Задание 6
SELECT * FROM store_simple.store;
SELECT category, COUNT(product_name) FROM store_simple.store GROUP BY category;

Задание 7
SELECT * FROM store_simple.store;
SELECT category, SUM(price*sold_num) result FROM store_simple.store GROUP BY category ORDER BY result DESC LIMIT 5;

Задание 8
SELECT * FROM project_simple.project;
SELECT COUNT(project_name), SUM(budget), AVG(DATEDIFF(project_finish, project_start)) FROM project_simple.project;Задание 1

SELECT * FROM billing_simple.billing WHERE payer_email = 'vasya@mail.com';

Задание 2

INSERT INTO billing_simple.billing VALUES (
	'alex@mail.com',
	'katya@mail.com',
	'300.00', 'EUR',
	'2016-02-14',
	'Valentines day present)');

SELECT * FROM billing_simple.billing
	WHERE payer_email = 'alex@mail.com'
		AND recipient_email = 'katya@mail.com'
        AND sum = 300.00;

Задание 3

SELECT * FROM billing_simple.billing;
update billing set payer_email = replace(payer_email, 'igor@mail.com', 'vasya@mail.com');

Задание 4

SELECT * FROM billing_simple.billing;
DELETE FROM billing WHERE payer_email IS NULL OR payer_email = "" OR recipient_email IS NULL OR recipient_email = "";



1.
CREATE TABLE book(
    book_id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(50),
    author VARCHAR(30),
    price DECIMAL(8, 2),
    amount INT);

2. 
INSERT INTO book ( title, author, price, amount)
VALUES ( "Мастер и Маргарита", "Булгаков М.А.", 670.99, 3);

3.
INSERT INTO book (title, author, price, amount)
VALUES ("Белая гвардия", "Булгаков М.А.", 540.50, 5),
 ("Идиот", "Достоевский Ф.М.", 460.00, 10),
 ("Братья Карамазовы", "Достоевский Ф.М.", 799.01, 2);




